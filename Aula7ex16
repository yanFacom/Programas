#include <stdio.h>
int main ()
{
	
	int 

	scanf ("%d", &m);

	for (i = 1, soma = 0, impar = 1; i <= m; i++,)
		soma = soma + impar
		if (i*i*i == soma)
			printf ("%d", soma)
		else 
			impar = impar + 2;

}